// import { Meteor } from 'meteor/meteor';
// import { HTTP } from 'meteor/http';

// Meteor.methods({
//   'cityPricesByState'(state) {
//     try {
//       const response = HTTP.get(`https://api.example.com/city-prices?state=${state}`); // API URL'sini buraya ekleyin
//       return response.data;
//     } catch (error) {
//       throw new Meteor.Error('api-error', 'API çağrısı başarısız oldu');
//     }
//   }
// });
